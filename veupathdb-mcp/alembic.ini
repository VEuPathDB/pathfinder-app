[alembic]
# The migration tree ships inside the package, so an installed distribution can
# create its own tables wherever it runs.
script_location = %(here)s/src/veupathdb_mcp/alembic
prepend_sys_path = src
path_separator = os

# The URL is read from EMBEDDING_DATABASE_URL / DATABASE_URL by env.py.
# sqlalchemy.url =
