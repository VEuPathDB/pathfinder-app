# Decisions

Choices with a real alternative, where the reasoning is not recoverable from the code. Each names what was rejected and why.

## Knowledge bundle

- [Upstream is the falsifier](upstream-is-the-falsifier.md) - why WDK reference material is admitted to a bundle that bans reference material

## Strategy graph

- [The nested tree stays at the wire boundary](nested-tree-at-the-wire-boundary.md) - why R1 did not flatten persistence
- [The combine sentinel is a boundary default, not a kind flag](the-combine-sentinel-is-a-boundary-default.md) - `__combine__` stays where `searchName` is required, and no reader asks it what a step is
- [Step status is derived, never stored](step-status-is-derived.md) - four states, not three, and no stored copy
- [A boolean operator is a type, not a string](boolean-operator-is-a-type.md) - one annotated type, so a bad operator is a 422
- [The local edit is the truth](local-edit-is-the-truth.md) - a WDK rejection is that step's problem, not the operation's
- [build_strategy is not revision-guarded](build-strategy-is-not-revision-guarded.md) - the exposure that was accepted, and the edit path that closed it
- [An EDA export begins the strategy when the thread has none](an-eda-export-begins-the-strategy-when-none-exists.md) - the blanket "Strategy AST missing" 404 is gone, so a first step from a study takes the same commit path the agent's first step takes
- [A strategy has a revision history, and fork, revert and Stop read it](a-strategy-has-a-revision-history.md) - every strategy write appends a snapshot, so a branch point, a revert target and a stopped turn each read the state in force at their message instead of the thread's latest AST
- [A strategy edit holds the thread's lock across its read](a-graph-operation-holds-the-thread-lock-across-its-read.md) - the read, the WDK round trip and the write of one JSON tree are one transaction on `/operations` and on `/insert-saved`, so two overlapping edits cannot each write a whole tree built from the same base

## Agents

- [Preserved is computed, never written](preserved-is-computed-never-written.md) - an edit turn's "the rest is unchanged" is read from a diff of the two specs, and an undeclared drop is a retry
- [The ledger outranks the verification digest](the-ledger-outranks-the-verification-digest.md) - a success the build does not support is rewritten where the digest is recorded, because a ModelRetry runs out and the same flag decides the memory auto-write and the eval verdict
- [An edit is a delta, not a rebuild](an-edit-is-a-delta-not-a-rebuild.md) - the diff becomes graph operations over the live strategy, so an untouched step keeps its WDK id and its hand edits
- [A built turn is asked to verify once](a-built-turn-is-asked-to-verify-once.md) - an output validator refuses the first answer of a turn that built and never verified, once per turn, so the second answer can state why a check is impossible
- [A budget stop is retried by the system, not the user](a-budget-stop-is-retried-by-the-system.md) - a pass that exhausted its calls records a typed stop the ledger names, is continued once by the dispatch when it made progress, and can no longer be reported to the user as the site being busy
- [A tool past its retry ceiling is answered, not removed](a-tool-past-its-retry-ceiling-is-answered-not-removed.md) - the offered toolset never shrinks and the ceiling returns the outage directive as the tool result, so a site outage costs a tool call and not the turn; removing the definition was rejected because the model has already chosen the name and the run reads the removal as an unknown tool at its retry maximum
- [Building is a response to a request](building-is-a-response-to-a-request.md) - the tools that change a strategy are off the Lead's list until the turn is classified as one that asks for a change
- [A reply states what the turn wrote](a-reply-states-what-the-turn-wrote.md) - the Lead answer carries a required strategy_changed flag that a validator checks against the turn write markers and refuses once in either direction; verb matching, a UI-only unchanged card and a defaulted flag were rejected
- [One turn contract, not seven validators](one-turn-contract.md) - the Lead's seven output validators became one typed `TurnRecord`, one pure `reconcile` and one validator that corrects a reply once with every mismatch it found; a validator per failure class, matching prose for the gene set's name and a UI-only marker were rejected
- [An off-topic turn reaches no tool](an-off-topic-turn-reaches-no-tool.md) - the classification the Lead makes first empties the tool list, pins a two-sentence redirect, caps the reply and lowers the turn's token ceiling; a prompt-only redirect and a pre-turn topic classifier were rejected
- [A clarification adds to the request](a-clarification-adds-to-the-request.md) - the thread accumulates every stated requirement, so a clarification turn frames from the original request as well as the answer
- [A criterion and its step are one address](a-criterion-and-its-step-are-one-address.md) - a build re-keys the spec on the step ids it minted, and a spec-less thread reconstructs one from its persisted AST
- [A multi-pick slot takes a list](a-multi-pick-slot-takes-a-list.md) - a `dict[str, str]` signature became "the API rejected it"
- [An override list stays a list](an-override-list-stays-a-list.md) - encoding at the tool boundary made the whole array one candidate option
- [The search listing carries names only](the-search-listing-carries-names-only.md) - `list_searches` returns the search names of a record type and nothing else, so the portal's 2769-search listing costs about 50248 estimated tokens instead of 143656 and no longer compacts the FRAME history on every step; a page size over the ontology order and a category axis on the served listing were rejected
- [A dependent vocabulary is read under its parents](a-dependent-vocabulary-is-read-under-its-parents.md) - the model was shown HB3's time points for a criterion bound to 3D7
- [A contextualized param view is an enrichment, and it has one owner](contextualizing-params-is-an-enrichment.md) - six implementations, four exception types, two with no policy at all
- [Enrichment of a gene list runs WDK's plugin, and the background is an organism](enrichment-by-value-runs-the-wdk-plugin.md) - the in-process exact hypergeometric was rejected because the annotated background lives in the site database, and a caller-supplied background list because the plugin refuses an organism its result does not contain
- [One proposer, one validator](one-proposer-one-validator.md) - the model reads the whole parameter sheet and answers every parameter once; the rules that read English and the per-parameter resolvers are deleted
- [A small model reads the request; embeddings only shortlist](the-model-reads-the-request-not-a-cosine-score.md) - the 0.45 threshold decided 108 of 237 uncovered params, and picked a phosphofructokinase domain; amended, since the resolver it introduced never bound
- [The two species lists are the proposal; the profile pattern is derived](phyletic-lists-are-the-proposal.md) - the hidden LIKE pattern is computed from what the model proposes, because that grammar answers a wrong value with a count
- [A value the request already states is not a question](a-value-in-the-request-is-not-a-question.md) - polarity, vocabulary-less identifiers, and an EC wildcard the regex could not match
- [An unmatched accession stops the chain](unmatched-accession-stops-the-chain.md) - PF00069 became phosphofructokinase because a miss fell through to similarity
- [A number's initial value is a default, not an example](numeric-default-is-not-an-example.md) - the free-text guard swallowed five WDK-declared defaults
- [An assumed value is recorded, not narrated](an-assumed-value-is-recorded-not-narrated.md) - a value FRAME chooses where the request says nothing becomes a non-blocking constraint the user can override; a contrast half stays a question
- [The strategy structure is a tree, because its shape is the science](structure-is-a-tree.md) - a left fold made `(B UNION C)` inexpressible and lost a gene
- [Chunk suppression follows the call, not a list of chunk types](suppression-follows-the-call-not-the-chunk-type.md) - the real cause of the recurring crash; the SDK threw on an orphan we emitted
- [Do not echo OpenAI item IDs back when you rewrite history](no-openai-item-ids.md) - the root cause of the tool-call-id crash on branch/revert/cancel/long loops
- [A retry must be something the model can act on](build-retry-must-be-actionable.md) - "call frame_problem first" looped when only the user could unblock
- [Eliding a tool result makes the agent fetch it again](elision-caused-refetching.md) - the context saver cost 12 duplicate calls in one turn
- [Prompts are checked against the architecture](prompts-match-the-architecture.md) - the base prompt named three schemas that do not exist
- [A saved conversation outlives the build that saved it](a-saved-conversation-outlives-the-build-that-saved-it.md) - the turn state drops fields the resuming build no longer declares, rebuilds the rest, and refuses plainly what it cannot read
- [Strict state, and the checkpoints flushed to allow it](no-checkpoint-truncation.md) - superseded: its loud refusal never reached the researcher
- The checkpoint allowlist binds at construction (`assistant-platform: docs/knowledge/decisions/the-checkpoint-allowlist-binds-at-construction.md`) - `with_msgpack_allowlist` discarded every declaration, so a state read warned on types that were on the list
- [A capability is not shipped until the model can find it](capability-must-be-reachable.md) - built, registered, and undiscoverable is not shipped
- [A sub-agent's approval is answered inside that sub-agent](sub-agent-approvals-re-enter-the-sub-agent.md) - the inner tool call is forwarded as its own approval card and the answer re-enters the suspended run
- [Every agent belongs to the turn that runs it](the-agent-belongs-to-the-turn.md) - the module singletons shared one `override`, and worker concurrency is about to rise
- [A saved strategy is a criterion's input, resolved when the criterion binds](a-saved-strategy-is-a-criterion-input.md) - FRAME lists the library through a service and the criterion carries the saved steps, so the FRAME to BUILD conversion stays pure; resolving at build time, answering with a WDK strategy id, and dropping an input the listing cannot resolve were rejected
- [The hidden-default sweep stops at published defaults](hidden-default-sweep-stops-at-published-defaults.md) - the nightly lane binds each required parameter from its own `initialDisplayValue` and dates what it could not reach; researching a plausible `channel` and `dataset_url` per search was rejected as 131 per-search studies whose answer expires, and the artifact already names every 422 and every 500

## Frontend

- [A parent term is a selection, so the tree expands it](parent-term-is-a-selection.md) - a correct organism scope rendered as "0 of 62 selected"

## Testing

- [Fixtures are built, not cast](fixtures-are-built-not-cast.md) - a cast also disables excess-property checks, which is how a deleted field survived
- [The dev server runs Turbopack](the-dev-server-runs-turbopack.md) - a proxied SSE stream keeps its gaps under both bundlers on Next 16.2.0, so the `--webpack` rule is retired
- [The e2e stack serves the production build](the-e2e-stack-serves-the-production-build.md) - the e2e overlay builds the `runner` target, so the suite meets no dev overlay and no growing compile cache; bounding the dev server's heap was rejected
- [Negative fuzzing is off only where the complement cannot be spelled](negative-fuzzing-needs-a-spellable-complement.md) - the conformance lane keeps negative cases on 79 of 94 operations; the parameter value union has no complement, so its fifteen are fuzzed positive-only
- [One injection judge, and what a failed judgement means](one-injection-judge-and-what-a-failed-judgement-means.md) - one judge serves both trust boundaries; a judgement that does not happen refuses the researcher's message with a 503 and withholds one tool result rather than ending the turn; the judge is built at start and reported as the `input_screening` subsystem only where the deployment screens, and the mock provider swaps its model for a scripted one that calls a single marker string an injection; failing open, a second judge and per-call usage telemetry were rejected

## Tooling

- [A silent anomaly must read the reply](silent-anomaly-must-read-the-reply.md) - the devtool judged prose handling without reading the prose
- [No faker or msw generation](no-faker-or-msw-generation.md) - reproduced, structural in the plugin, and it fights the real-data testing rule
- [One way to generate types](one-way-to-generate-types.md) - the duplicate codegen path was broken and was deleted, not fixed
- [The dead-code checker is a pinned dependency, and its whitelist is one config line](the-dead-code-checker-is-a-pinned-dependency.md) - the 70 "invalid syntax" lines were an unpinned binary, not vulture's parser, so the checker is wired instead of deleted and the 49-entry whitelist that decided nothing is gone
- [The docs build creates its extra="allow" models before autodoc runs](extra-allow-models-are-built-before-autodoc-runs.md) - Sphinx's type-comment pass writes `__pydantic_extra__` back into `BaseModel.__annotations__`, and every model built after that fails; patching Sphinx and accepting 40 empty pages were both rejected

## Transport

- [NUL is rejected at the ASGI boundary](nul-rejected-at-the-asgi-boundary.md) - why not a validator, and why not an exception handler
- [A write the caller reads back commits before its response](a-write-commits-before-its-response.md) - the session dependency commits after the response, and an in-process transport cannot see the race
- [The thread title is the last chunk before a turn finishes](the-thread-title-is-the-last-chunk-before-finish.md) - the title used to ride the first graph chunk that arrived after its task finished, so the same turn put it at index 4 or at index 7; it is now awaited before `finish` under a 15 s ceiling
- [The API rewrite carries a long call](the-api-rewrite-carries-a-long-call.md) - Next's 30 s rewrite cap answered the data purge with its own bare 500
- [A VEuPathDB bearer token is the user; a service token is the application](bearer-identity-and-service-tokens.md) - the ES512 JWKS protocol, and why `proxied-user-id` cannot serve a service that acts as the user
- [A resource is owned by a user under one application](application-id-tenancy.md) - the scope key, isolated memories, a per-user cap with per-application attribution, and why not one user row or one database per application
- [A conversation is a thread; its strategy is an attachment](conversation-thread-and-strategy-split.md) - the column partition, absent-row semantics, one eager loader, and why a nullable column is not an attachment
- Part kinds keep their names when the taxonomy opens (`assistant-platform: docs/knowledge/decisions/part-kinds-keep-their-names.md`) - the kind is persisted in every stored chunk, so namespacing waits for the assistant that needs it
- [The runtime takes the vocabulary as an argument; the wire keeps it](vocabulary-is-an-argument.md) - roles, guard tool sets and memory kinds became arguments; the published enums stay narrow until a client needs them open
- [The assistant runtime is a package boundary, not a contract over scattered modules](assistant-core-is-a-package-boundary.md) - superseded; the modules moved into `assistant_core/` and contract 7 rejected indirect chains too, because a config-only boundary has to be rediscovered on every extraction
- [The runtime is a package, so the boundary is an installation fact](the-runtime-is-a-package.md) - `assistant-platform: packages/assistant-core` with its own pyproject, lock, tests and lane; contract 7 became a dependency list, the thread moved with it, and in-repo contracts only were rejected
- [The client library owns its foundation, so no edge points up out of it](the-client-library-owns-its-foundation.md) - the 84 imports from the future `veupathdb-py` module set into `assistant_core` and `pathfinder.platform` became 0 in place; a fifth shared distribution for `CamelModel` and the JSON aliases was rejected, and `veupathdb_auth_token_ctx` moved down because the client is what names a WDK bearer
- [The MCP server writes no PathFinder table, so a second deployment can run it](the-mcp-server-writes-no-pathfinder-table.md) - the 54 imports from the future `veupathdb-mcp` module set into the application became 0 in place, the served closure fell from 162 modules to 133 and names no PathFinder table; an `ExportSink` protocol was rejected because the function's only callers are the application's, and a read-only connection because it keeps the schema dependency
- [The embedding index belongs to the MCP unit, not to the assistant runtime](the-embedding-index-belongs-to-the-mcp-unit.md) - no module inside `assistant_core` imported the record manager, so it and the two tables `embedding_vectors` / `embedding_index_entries` moved to `veupathdb_mcp/embeddings/` on a base of their own and the MCP unit copied the embedder; making `veupathdb-mcp` depend on `assistant-core` for a pgvector table was rejected, and so was a fifth shared distribution
- [The runtime's part payloads live in the runtime, so the package builds alone](runtime-part-payloads-live-in-the-runtime.md) - the durable-task payloads and `TurnUsage` moved out of `pathfinder-shared` and the dependency is gone; publishing `pathfinder-shared` as a second distribution was rejected because it has no second consumer, and the registry left `__init__.py` because a package that imports on load cannot hold a module its own importers need
- The wire protocol is a written spec, verified against captured frames (`assistant-platform: docs/knowledge/decisions/the-wire-protocol-is-a-written-spec.md`) - `PROTOCOL.md` states the rules a client must follow and a package test owns its facts; generating the page from the models was rejected because a generator has nowhere to put a rule
- [A staged eval case carries its user until promotion, and a promoted case carries nobody](a-staged-eval-case-carries-its-user-until-promotion.md) - the linkage has a lifetime and a check constraint enforces it; storing nothing was rejected because an opt-out could clear nothing, keeping it after promotion because the corpus is what ships
- [The eval harness is pydantic-evals, and the summary shape is ours](the-eval-harness-is-pydantic-evals.md) - the dataset, the case loop and the evaluator protocol come from the library; the SLI feed does not, and a graded tree distance waits for a corpus that needs one
- [The orchestration belongs to the assistant, not to the platform](the-orchestration-is-the-assistants.md) - `AssistantSpec` carries the graph factory, state, parts, kinds, mock and identity gate; one platform graph parameterized by config was rejected because a config-shaped Lead is still a Lead
- The admitted tool sources are installed by the host (`assistant-platform: docs/knowledge/decisions/admitted-tool-sources-are-installed-by-the-host.md`) - a module-level seam the host calls at start; a turn-seam parameter was rejected because a request must have no channel to name a server, and `RuntimeSettings` because a dropped env var would admit nothing in silence
- A tool source's session belongs to the turn (`assistant-platform: docs/knowledge/decisions/a-tool-source-session-belongs-to-the-turn.md`) - `ResolvedToolSources` opens and closes every declared source around the whole drive; the assistant's graph owning the entry was rejected because a graph that raises leaks the session, and the library's per-run entry because a turn with several agent runs would open a connection per run
- A one-agent turn runs over the thread's own messages (`assistant-platform: docs/knowledge/decisions/the-thread-carries-its-own-messages-across-turns.md`) - the checkpoint carries the run's messages, trimmed to the last complete exchange; rebuilding them from the durable chunk log was rejected because the wire does not carry the pairing, and a runtime window because no thread has yet measured too expensive
- The client package has three rings, and the innermost one has no dependencies (`assistant-platform: docs/knowledge/decisions/the-client-is-a-package-with-three-rings.md`) - a dependency-free protocol core, one AI-SDK-coupled transport, and a named legacy module for the task dialect; one SDK-importing module was rejected because a non-SDK host would inherit it to parse a frame
- [A durable task reports itself on the thread, coarsely, and the per-task channel is deprecated](durable-task-progress-belongs-in-the-thread-log.md) - progress and completion join the log with the same cursor as every other chunk; a second dialect forever, every tick, and deleting the route were all rejected, each for a different reason
- [A durable tool is a deferred tool, so its completion continues the turn](a-durable-tool-is-a-deferred-tool.md) - the tool raises `CallDeferred`, the call parks on the state, and the worker opens a new turn carrying `DeferredToolResults`; LangGraph `interrupt()` inside the node was rejected because a resume re-executes the node from its first line, which measured two analyses and three compute jobs for one prompt
- [The user sees studies, searches, strategies and steps, never EDA, WDK, FRAME, BUILD, VERIFY, Lead, sub-agent or Ledger](user-facing-vocabulary.md) - the glossary every on-screen string uses, enforced by a source scan on each side; identifiers only where the user typed them
- [A WDK-backed feature requires a registered VEuPathDB login](wdk-requires-registered-login.md) - one 401 code for the refusal, guest minting deleted, the service account confined to user-independent reads, and why not one shared identity for anonymous users
- [The MCP server verifies with the API's own bearer verifier, and publishes RFC 9728](mcp-auth-reuses-the-api-verifier.md) - three credential modes, a separate service-token registry, and the SDK's protected-resource document; a second JWKS client and an inbound `VEUPATHDB_AUTH_TOKEN` were rejected
- [The wdk-mcp server is a product module of the api, served from the api image](the-wdk-mcp-server-is-a-product-module.md) - `pathfinder/mcp/` calls the services and the import contract holds the line; a standalone package, a mount inside the FastAPI app, and exporting the `ai/tools` wrappers were rejected, and a tool that cannot fit 60 seconds declares its own floor in `_meta`
- [The WDK service layer holds functions, not re-exports](the-wdk-service-layer-holds-functions-not-re-exports.md) - `veupathdb_mcp/wdk/__init__.py` exports nothing, the client handouts became functions with a caller's signature, and the four FRAME imports of the wire search model are named in the layering contract with a test pinning the symbol; keeping the twenty-four name alias facade was rejected because it hid the exception and admitted every other name with it
- The conformance suite is a distribution of its own, and a skipped check is not a pass (`assistant-platform: docs/knowledge/decisions/the-conformance-suite-is-a-separate-distribution.md`) - `veupathdb-mcp-conformance` ships its six families inside a pytest plugin that imports nothing of this deployment; the api test tree, `assistant-core` and a container-only artifact were rejected, and the account-state extension point is a pytest hook because a fixture cannot be overridden from outside an installed package
- [The memory ceilings bound the worker and wdk-mcp, and leave the api uncapped](the-memory-ceilings-bound-the-growers-not-the-api.md) - 2g each on the two processes whose memory grows with sites touched, measured against the api's 6.54 GiB idle-warm; a 3g wdk-mcp ceiling was rejected after it let the kernel OOM-kill the api, and no api ceiling both fits the VM and clears its peak
- [A site that is down is down on its own](a-site-that-is-down-is-down-on-its-own.md) - readiness is every process subsystem plus one loaded catalog, and a site whose catalog did not load is degraded: named in the health body and in `GET /api/v1/sites`, refused on its own routes with 503 `SITE_UNAVAILABLE`, retried on an interval, marked "Not responding" in the site menu and on the rail trigger, never the site a site-less path opens, kept on its own app shell so its local rows stay reachable, and read for a WDK identity only while it answers; requiring every catalog was rejected, and so was resolving every identity against the configured default site, because both let a portal outage take the deployment down
- [Per-site catalogs are evicted under a budget, and the warm-up does not block the bind](per-site-catalogs-are-evicted-and-the-warm-up-does-not-block-the-bind.md) - an LRU over `SITE_CATALOG_BUDGET_MB`, one catalog build at a time and only in the api, snapshots on a named volume, and a lifespan that spawns the warm-up; one index-holding process, parallel encoding, a rebuild inside a capped container, readiness as the healthcheck and an RSS-derived budget were rejected
- [Embeddings are an API call and a Postgres record manager](embeddings-are-an-api-and-a-record-manager.md) - the local fastembed model is deleted; `text-embedding-3-large` at 1024 dimensions, batched and parallel, with the vectors in two Postgres tables addressed by their text, the api syncing and the others searching; keeping fastembed with build-time `.npz` caches, `bge-small` and a quantized nomic were rejected
- [PathFinder admits veupathdb-wdk-mcp from two settings, and only with both](pathfinder-admits-its-own-mcp-server-from-two-settings.md) - the record is built in code and the endpoint and its credential are the only settings, so half a configuration admits nothing; one JSON environment variable was rejected because a deployment's guarantees cannot arrive as a parsed string, and installing from the cached assistant registry because the order would depend on which caller was first
- [The research tools are served by a second server of the wdk-mcp distribution](two-servers-from-one-distribution.md) - literature and web search left the app for `veupathdb-research-mcp` on 8110, one repository and one image with two targets, its own credentials and the `data-research.sources` namespace; a new repository for the research server, and one mixed server serving both tool groups, were rejected
- [A declared tool source reaches a one-agent assistant through its deps](a-declared-source-reaches-a-one-agent-assistant-through-its-deps.md) - site help carries the turn's toolsets on its own context and deps and names them with a run-context toolset function; widening the runtime's agent factory was rejected because the graph is compiled before the sources resolve, and a context variable because the assistant already has a typed per-turn channel
- [One id names one message in the log](one-id-names-one-message-in-the-log.md) - the dispatcher appends a user envelope only when the log has none for that id and the reducer keeps the first message an id names; a server-minted id, branching on `trigger`, a unique index and a repair of existing rows were all rejected
- A resumed stream reads one turn, and the transport holds the rest (`assistant-platform: docs/knowledge/decisions/a-resumed-stream-reads-one-turn.md`) - the SDK reads one message per stream and seeds it with the message the client holds, so the transport stops at the `start` that opens the next turn and serves the rest to the next resume; merging the continuation into the suspending message, a second HTTP tail, and an `id` on `data-background-task-started` were all rejected
- [experimentId is this host's own request extension](experiment-id-is-this-hosts-request-extension.md) - the wire ships an empty extension table, so the one field this application adds to the chat body is documented and gated here; naming it in the shipped protocol and dropping the field were both rejected
- [A revert is seen by re-snapshotting, not by a protocol signal](a-revert-is-seen-by-re-snapshotting.md) - the client refetches the snapshot on the 204 and replaces its list before sending the edit; a truncation chunk in PROTOCOL 1, fork-as-new-thread, and a local truncation of the client's own list were all rejected
- [Our own admission record reads incomplete, because one account cannot prove isolation](our-own-admission-record-reads-incomplete.md) - the registered account is the first credential and the service token the second, so the leak rule covers both; making the service token the owning identity would settle isolation and was rejected because every other check would then run as a credential WDK refuses
- [The EDA permissions cache is keyed by the credential the call carries](the-eda-permissions-cache-is-keyed-by-the-credential.md) - the site plus a hash of the request's token, so two accounts in one process hold two authorization maps; keying by the WDK user id from `/users/current` was rejected because the round trip buys only cross-token dedup and puts a WDK dependency in a pure-EDA read
- [A durable task carries the deferring turn's phase picks](a-durable-task-carries-the-turn-s-phase-picks.md) - a `phase_overrides` column on `background_tasks`, written from a per-turn context variable and read back when the worker opens the completion turn; persisting them on the conversation was rejected because it turns a per-request override into a thread setting
- [A dead worker fails its turn by heartbeat, at one minute of silence](a-dead-worker-fails-its-turn-by-heartbeat.md) - the sweep releases the jobs of a worker silent past 60 s as well as the jobs past the started-age timeout, Stop releases the same job on the spot, and a released turn closes its open tool calls first; the beat runs in a thread of its own, measured 0.315 s old inside a job that held the event loop for 5 s against 5.028 s without it, and a beat on the worker's own loop was rejected for beating zero times in that window
- [A memory-store call is bounded by the turn, and the two calls fail differently](a-memory-store-call-is-bounded-by-the-turn.md) - retrieval and the auto-write run under `memory_store_timeout_seconds` (30 s); retrieval degrades to no memories, the auto-write re-raises into the turn's error path, and the store's batch task now ends with the store
- [The thread log is the EDA binding's history, and a branch never shares a document](the-thread-log-is-the-eda-bindings-history.md) - fork and revert read the newest `data-eda.analysis-state` part and put the binding where it says, creating a document of the branch's own from the recorded descriptor; a binding revision table, copying the `conversation_analyses` row, the service's copy route and deleting a replaced document were all rejected
- [A frozen acceptance suite is not a test tier](a-frozen-acceptance-suite-is-not-a-test-tier.md) - the four suites no script, hook or CI job collected are deleted and the four contracts only they pinned moved into the live tests; a nightly workflow was rejected because every module skipped clean when its target vanished, and the no-edit rule that made them useful mid-batch is what stops them following the code afterwards
- [VDI is a publish target, not a store and not a search input](vdi-is-a-publish-target-not-a-store.md) - an opt-in button publishes a gene set to the researcher's VEuPathDB workspace and the row keeps the dataset id; replacing the `gene_sets` table with VDI, feeding a VDI id into a WDK `DatasetParam`, an agent tool and a browser-side call were all rejected on measured evidence
- [The client library is a distribution, so PathFinder cannot reach into it](the-client-library-is-a-distribution.md) - the WDK and EDA clients, their foundation and the WDK-shaped half of `domain/` are their own repository, consumed by URL at a commit, with import-linter contracts 1 and 4 replaced by the package's dependency list and its own boundary suite; keeping the client in-repo behind import contracts was rejected because the folder is being published as its own repository
- [The MCP server is a distribution, and PathFinder is one of its consumers](the-mcp-server-is-a-distribution.md) - the WDK catalog, parameter and gene tools, their MCP transport and the semantic index they own are their own repository, consumed in process by URL at a commit and carrying an alembic chain of its own; keeping the server inside `apps/api`, and making PathFinder an MCP client of the served process now, were both rejected, the second at a measured cost of 132 production edges
- [The assistant platform is a grouping of three distributions, not one](the-assistant-platform-is-a-grouping.md) - `assistant-core`, the TypeScript client and the conformance suite move into a repository of their own, with `PROTOCOL.md` inside the runtime package and zero import rewrites; merging them into one distribution was rejected because they ship to different consumers, in two languages, on different release clocks
- [The two knowledge bundles cite each other, they do not link](the-knowledge-bundles-cite-each-other-they-do-not-link.md) - a markdown link that leaves a bundle becomes a backticked citation of the sibling repository path, in both directions, and the two PathFinder proposals under EDA move back into this bundle; GitHub URLs into the other repository were rejected because an unpinned branch URL rots as silently as a dead relative path and a pinned one goes stale on the first edit
- [The workbench is a module, not a repository](the-workbench-is-a-module-not-a-repo.md) - `features/analysis` moved inside `features/workbench` and the merged feature publishes two entry paths that `check-boundaries.mjs` rule 6 enforces, while `services/workbench/` is a facade of functions that import-linter contract 8 makes the only way `pathfinder.ai` and `pathfinder.jobs` reach the workbench services; a repository of its own now was rejected because no second consumer and no second container exist, and leaving the two features apart because an exception row naming a feature admits every file in it
- [An enrichment over a result of several organisms is refused until a background is named](an-enrichment-over-several-organisms-is-refused.md) - WDK's enrichment plugins test one organism against its own genome and the form defaults to the first organism in the result, so a 10-gene ToxoDB set over 7 organisms was tested as 1 Eimeria gene and read "0 significant terms"; `EnrichmentService` now refuses a multi-organism result with no named background and lists the organisms; the form default, a majority organism and one run per organism were rejected
- [The three libraries are consumed by URL at a release](the-libraries-are-consumed-by-git-url.md) - `apps/api` names the four Python distributions by repository URL and a release tag, `apps/web` names the client by repository, workspace and tag, and the `wdk-mcp` image builds from the MCP repository at the same release, committed in its compose build context; subtree mirrors, an internal package index that does not exist and GitHub Packages were rejected, the last because it serves no Python index and asks for a token to read a public npm package
- [A question a search answers is a build](a-question-a-search-answers-is-a-build.md) - a count or a membership question about genes is built and read off the step, not looked up on the web.
